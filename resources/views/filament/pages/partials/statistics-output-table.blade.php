<table class="stats-table"><thead><tr><th>Name</th><th>Quotes</th><th>Schedules</th><th>Packs</th><th>Total</th>@if(auth()->user()->can('pricing.view'))<th>Quote value (net / gross)</th>@endif</tr></thead><tbody>@forelse($rows as $row)<tr><td>{{ $row['name'] }}</td><td>{{ $row['quotes'] }}</td><td>{{ $row['schedules'] }}</td><td>{{ $row['packs'] }}</td><td>{{ $row['total'] }}</td>@if(auth()->user()->can('pricing.view'))<td>@forelse($row['values'] as $code=>$value)<div>{{ \App\Models\Project::symbolForCurrency($code) }}{{ number_format($value['net'],2) }} / {{ \App\Models\Project::symbolForCurrency($code) }}{{ number_format($value['gross'],2) }}</div>@empty—@endforelse</td>@endif</tr>@empty<tr><td colspan="6">No outputs in this period.</td></tr>@endforelse</tbody></table>
